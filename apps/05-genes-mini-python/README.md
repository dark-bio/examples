# 05 - genes-mini (Python)

[05-genes-mini-rust](../05-genes-mini-rust) written in Python. It takes the
sequence length from `os.stat` and reads an absent scalar as no answer when
`FileNotFoundError` arrives. [01-hello-python](../01-hello-python) covers what a
Python module costs.

## Build and run

```sh
make run APP=05-genes-mini-python
```
