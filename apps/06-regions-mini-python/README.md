# 06 - regions-mini (Python)

[06-regions-mini-rust](../06-regions-mini-rust) written in Python. It reads the
sequence in 8 KiB chunks of bytes, so the interval never has to fit in memory.
[01-hello-python](../01-hello-python) covers what a Python module costs.

## Build and run

```sh
make run APP=06-regions-mini-python
```
