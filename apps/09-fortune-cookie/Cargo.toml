[package]
name = "fortune-cookie"
version = "0.2.0"
edition = "2024"

# A smaller module uploads and starts faster on an Ark.
[profile.release]
opt-level = "z"
lto = "fat"
codegen-units = 1
panic = "abort"
strip = "symbols"
